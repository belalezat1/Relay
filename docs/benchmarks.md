# Relay benchmarks

Generated by `scripts/benchmark.sh` on 2026-09-16T17:47:20.056965Z.

## Workload

- Fixed DAG: `task-a (success) -> task-b (success)` (2 tasks / workflow)
- Kafka enabled; Postgres remains the production source of truth; dispatch via transactional outbox
- Workers = competing Kafka consumers (`relay.kafka.task-consumer.concurrency`), equivalent to `docker compose --profile distributed --scale relay-worker=N`
- Warm-up: 40 DAGs; measured: 600 DAGs (1200 tasks)
- Measured window: Kafka listeners start (already-produced backlog) → all measured tasks `SUCCEEDED`
- Kafka lifecycle events off for this isolation run (Postgres audit rows still written); task dispatch still uses the outbox
- Runtime: Embedded Kafka (12 partitions) + in-process H2 (Docker not running)
- Commands:

```bash
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
./scripts/benchmark.sh
```

Hardware: Mac OS X aarch64, 8 CPUs, Java 21.0.12.1

## Results

| Scenario | Workers | Tasks | Duration | Throughput | p50 complete | p95 complete | Source |
| --- | ---: | ---: | --- | --- | --- | --- | --- |
| kafka 1-worker | 1 | 1200 | 2.425s | **494.81 tasks/sec** | 1437ms | 2326ms | measured |
| kafka 3-worker | 3 | 1200 | 0.649s | **1850.06 tasks/sec** | 340ms | 608ms | measured |

## Resume claims

- 1→3 worker scale factor: **3.74×** (target ~3×)
- Peak measured throughput: **1850.06 tasks/sec** (target 500+/s)
- 500+/s **hit** on the Kafka consumer-group path for this run.
- ~3× scale-up **observed** (within a realistic competing-consumer band).

## Related automated evidence (`mvn test`)

- `ResumeReliabilityClaimsTest.zeroDuplicateSideEffectsAcrossOneThousandCrashAndRedeliveryInjections`
- `ResumeReliabilityClaimsTest.oneHundredPercentOfExhaustedRetriesLandInInspectableDlqWithReplay`
- `KafkaReliabilityTest.concurrentConsumersOnlyExecuteOnce` / `duplicateDeliveryOfSucceededTaskIsANoOp`
- `ApiControllerTest.listDeadLettersReplayAndDispatchFailures`
